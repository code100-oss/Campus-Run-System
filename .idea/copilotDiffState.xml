<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/src/fosu/UserService.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/fosu/UserService.java" />
              <option name="originalContent" value="package fosu;&#10;&#10;public class UserService {&#10;    // 简单模拟用户登录校验&#10;    public boolean login(String username, String password) {&#10;        // 实际项目应查数据库，这里仅做演示&#10;        return &quot;admin&quot;.equals(username) &amp;&amp; &quot;123456&quot;.equals(password);&#10;    }&#10;}&#10;&#10;" />
              <option name="updatedContent" value="package fosu;&#10;&#10;public class UserService {&#10;    // 简单模拟用户登录校验&#10;    public boolean login(String username, String password) {&#10;        // 实际项目应查数据库，这里仅做演示&#10;        return &quot;admin&quot;.equals(username) &amp;&amp; &quot;123456&quot;.equals(password);&#10;    }&#10;&#10;    /**&#10;     * 尝试修改用户密码。验证旧密码是否匹配，并对新密码做最基本的校验（非空且长度&gt;=6）。&#10;     * @param user 待修改密码的用户&#10;     * @param oldPassword 旧密码（用于校验）&#10;     * @param newPassword 新密码&#10;     * @return 修改成功返回 true，否则返回 false&#10;     */&#10;    public boolean changePassword(User user, String oldPassword, String newPassword) {&#10;        if (user == null) return false;&#10;        String current = user.getPassword();&#10;        if (current == null || !current.equals(oldPassword)) {&#10;            // 旧密码不匹配&#10;            return false;&#10;        }&#10;        if (newPassword == null || newPassword.length() &lt; 6) {&#10;            // 新密码不符合最小长度要求&#10;            return false;&#10;        }&#10;        user.setPassword(newPassword);&#10;        return true;&#10;    }&#10;}&#10;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>